# **Installation**
### install pipenv
```shell
python -m pip install pipenv
```

### launch pipenv environment
```shell
pipenv shell
```

### install required dependencies from Pipfile
```shell
pipenv install
```

### run game
```shell
python main.py
```
